import { createBrowserClient } from "@supabase/ssr";

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL! ?? "https://vokwpfdmdzipaidjtoza.supabase.co",
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY! ?? "sb_publishable_juITTTlOPA-Nelr6H5olpg_8pthN0UT"
  );
}